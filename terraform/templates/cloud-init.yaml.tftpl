#cloud-config
hostname: ${hostname}
manage_etc_hosts: true
users:
  - name: ${ssh_username}
    groups: [adm, sudo]
    shell: /bin/bash
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    ssh_authorized_keys:
      - ${public_key}
ssh_pwauth: false
package_update: false
${additional_cloud_init}
